export interface LoginForm {
    email: string;
    password: string;
  }

  export interface RegisterForm {
    FirstName: string;
    LastName: string;
    email: string;
    password: string;
    confirmpassword: string;
  }

